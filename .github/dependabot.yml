version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    # This is a single-purpose reproduction repo, not a maintained package -- it doesn't need routine dependency-bump PRs, and their CI runs compete for the same limited macOS runner concurrency the actual repro workflow needs.
    open-pull-requests-limit: 0
